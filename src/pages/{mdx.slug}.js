import * as React from 'react'
import Layout from '../components/layout'

const BlogPost = () => {
  return (
    <Layout pageTitle="MDX Slug Template Blog Posts">
      <p>Template made with `&#123;mdx.slug&#125;` syntax.</p>
    </Layout>
  )
}

export default BlogPost

